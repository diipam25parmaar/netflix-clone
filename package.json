{
    "name": "netflix-clone",
    "private": true,
    "version": "0.0.1",
    "workspaces": ["packages/*"],
    "scripts": {
      "dev": "pnpm -w -r --parallel run dev",
      "build": "pnpm -w -r run build"
    }
}